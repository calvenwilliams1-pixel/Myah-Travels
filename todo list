# MyCalTravels — TODO.md (Revision 3)

## Status

**Portal System V1 + V2 COMPLETE. Quick Wins + Itinerary Builder + Admin Notepad all shipped. Ready for testing + Phase 7 planning.**

## Core Principle

Developer controls design.
Template controls layout.
Writer controls content.
Settings control brand.
System controls hierarchy.

---

## Locked Decisions

### APPROVED
- Template System (structural only)
- Block Registry (10 blocks for posts)
- Block Editors + Renderers (switch-based)
- Live Preview (sticky split screen)
- Semantic theme tokens (primary, secondary, accent, success, warning, danger, info)
- Curated colour palettes (6 pre-validated)
- Fixed white background (professional standard)
- Status colours fixed (never themeable)
- **Content Library** (create once, reuse everywhere)
- **Portal Wall** (travel information wall, client-facing)
- **Admin Preview** (bypass magic link for admin)
- **Hero Banner** with 8 named presets (no colour pickers)
- **Itinerary as portal_items** (unified pipeline, no merge)
- **Flights = Travel segments** (no separate table)
- **Notepad is admin-only** (never rendered to clients)
- **Print stylesheet for itinerary export** (no PDF pipeline)
- **Empty days render placeholder** (never blank)

### REJECTED
- CanvasElement for writers
- Moveable for writers
- Drag-anywhere layout
- Shape editors
- Border controls
- Property panels
- General-purpose design tools
- Full colour customisation (4 pickers)
- Free-form background colour picker
- Brand-aware dark mode
- **Standalone Itinerary data source** (unified via `portal_items`)
- **Itinerary PDF pipeline** (print stylesheet only)
- **Options segments in V1** (deferred to V2)
- **Cruise mode in V1** (deferred to V2)
- **People table in V1** (trigger-based upgrade)
- **Notepad cross-portal sharing** (strictly scoped)
- **Merging data sources on the wall** (single pipeline)
- **Notices feature** (removed — redundant with Alert category)
- **TagInput post-tag backend coupling** (UI pattern only)

---

## Phase 1: Block Foundation ✅ COMPLETE
- [x] BlockType discriminated union (10 blocks)
- [x] BLOCK_REGISTRY
- [x] TemplateSection (label + state)
- [x] TemplateStyle
- [x] RGB CSS variables
- [x] Semantic Tailwind tokens
- [x] ThemeProvider wraps public + admin

## Phase 2: Content Blocks ✅ COMPLETE
- [x] 10 block editors
- [x] 10 block renderers
- [x] PostRenderer (switch, 10 cases)
- [x] BlockEditor
- [x] TemplatePreview
- [x] Zero type errors, build passes

## Phase 3: Template Creator ✅ COMPLETE
- [x] TemplateCreator UI
- [x] Template store
- [x] Admin route `/admin/templates`
- [x] Nav link + QuickAction
- [x] 3 starter templates

## Phase 4: Theme System ✅ COMPLETE
- [x] Removed secondary + background pickers
- [x] Secondary derived from primary
- [x] 6 curated palettes + PalettePicker
- [x] All 225 hardcoded colours migrated
- [x] Rename to MyCalTravels

## Phase 5: Portal V1 ✅ COMPLETE
- [x] `content_library` table + admin UI
- [x] `portal_items` table (library + portal-specific)
- [x] Portal Wall rendering (pdf/image/text)
- [x] Hero Banner (title, subtitle, image, 3 presets)
- [x] Admin Preview route
- [x] PortalWall extracted as pure rendering component
- [x] Hero image upload
- [x] Preview Wall button on edit page
- [x] `scripts/setup-db.js`
- [x] `schema.sql` supports migration chain

## Phase 6.1: Quick Wins ✅ COMPLETE
- [x] Hero preset expansion (3 → 8)
- [x] Content tile icons (category → emoji)
- [x] Notices removal (UI + action)
- [x] Portal member name collection

## Phase 6.2: Itinerary Builder V1 ✅ COMPLETE
- [x] Schema: itineraries, sections, days, segments, stays
- [x] `portal_items.itinerary_id` + `source_type='itinerary'`
- [x] Migration 0003 with portal_items rebuild
- [x] Backend repository (`lib/itineraries`)
- [x] Validation (Zod + travel datetime ordering + overlap detection)
- [x] 11 API routes
- [x] Admin itinerary editor (list + editor pages)
- [x] Client itinerary view (time-grouped, type-coloured)
- [x] Travel cards for flights
- [x] Stay banner ("where you're sleeping tonight")
- [x] Empty-day placeholder
- [x] Print stylesheet
- [x] Wall integration via unified pipeline
- [x] Preview itinerary from admin

## Phase 6.3: Admin Notepad ✅ COMPLETE
- [x] Migration 0004: `notepad_entries`
- [x] Backend repository + search + distinct tags
- [x] API routes (list, search, create, update, delete)
- [x] Admin UI (list, add, search, autocomplete)
- [x] Data guardrail banner
- [x] TagInput UI pattern (no post-tag backend coupling)
- [x] Navigation buttons on portal detail page

---

## Phase 7: Testing + Polish (Current Focus)

### 7.1 Full Test Pass (Local PC)
- [ ] Content Library: add PDF/Image/Text, filter, delete
- [ ] Portal Editor: hero presets, attach items, reorder, preview
- [ ] Wall: magic link flow, hero, icons, itinerary tile
- [ ] Itinerary Builder: create → section → day → segment
- [ ] Travel segment flight fields
- [ ] Client itinerary view (time grouping, colours, stays)
- [ ] Empty day placeholder
- [ ] Print / Save PDF
- [ ] Notepad: add, tag, search
- [ ] Regressions: settings, login, homepage feed
- [ ] Fix any bugs found

### 7.2 Loose Ends (Before Phase 8)
- [ ] Attach itinerary UI on admin editor page (currently only on list page)
- [ ] Admin preview of itinerary (currently requires portal_session)
- [ ] Verify travel segment `datetime-local` ↔ ISO string handling
- [ ] Verify PDF export includes proper timestamp

### 7.3 Production Hardening
- [ ] Fix better-sqlite3 crash on mini PC deployment
- [ ] Delete Canvas system (39 emerald remain)
- [ ] Preview popup (modal → floating → fullscreen)
- [ ] Target surface token conversion (~20-30 `bg-gray-50/100`)
- [ ] Auto Save (debounced, 2s)
- [ ] Undo / Redo (global stack, capped 50)
- [ ] Publish Validation (required blocks filled)
- [ ] Error Boundaries per block
- [ ] Responsive Preview (desktop/tablet/mobile)

---

## Phase 8: Future / Deferred

### Itinerary V2
- [ ] Options segments (A/B alternatives on free days)
- [ ] Cruise mode (port schedule tables)
- [ ] Overlapping stays
- [ ] Group splits
- [ ] Attendees on segments (deferred indefinitely — lives in Notepad)

### People & Tagging V2
- [ ] People table (triggered by naming inconsistency)
- [ ] Per-person itinerary lookup
- [ ] PII isolation (separate from post-tags)

### Portal Enhancements
- [ ] Client portal reply capability
- [ ] Global announcements (send email to all portal members)
- [ ] Client-facing docs acknowledgment

### Content Migration
- [ ] Template Migration (Canvas → Block)
- [ ] Inline Editing (blocks)

### Low Priority
- [ ] Dark mode (reader preference only)
- [ ] Template versioning
- [ ] Reading time / word count
- [ ] Additional block types (20+ threshold before registry refactor)

---

## Architecture Decisions (Locked)

### Block System (Posts)
- 10 blocks: Title, Body, Callout, Hero, Image, Gallery, QuickFacts, Quote, ProsCons, Verdict
- Switch-based rendering
- Discriminated unions
- Renderers receive `{ data, style }`

### Theme System
- Primary + Accent = user-picked (2 pickers)
- Secondary = derived from primary
- Background = fixed white
- Status colours = fixed
- 6 curated palettes
- 8 hero presets

### Content Library
- `content_library` table
- Categories = filtering only (never rendering)
- Referenced by `portal_items`, never copied
- Type: `pdf` | `image` | `text`

### Portal Wall
- Single pipeline via `portal_items`
- `source_type`: `library` | `portal_specific` | `itinerary`
- Rendering by `type`: pdf | image | text | itinerary
- Wall queries one table + resolves references
- Hero: image takes precedence over preset

### Itinerary
- Separate content type, NOT Portal Blocks
- Chronology-driven (not prominence-driven)
- Travel segments carry flight data (no separate table)
- Stays belong to one section (no cross-section)
- Sequential stays only
- Time-derived ordering (no manual reorder)
- Empty days render placeholder
- Print stylesheet export (no PDF pipeline)
- Attached via `portal_items.source_type='itinerary'`

### Notepad
- Admin-only, never rendered to clients
- Comma-separated tags (V1), People table (V2 trigger)
- Logistics notes only (no diagnostic detail)
- TagInput UI pattern only, no post-tag backend coupling
- Search by content or tag

### Templates
- Structural only
- Live templates (edit → all posts update)
- `version: 1` in layoutData

---

## Guardrails (Do Not Violate)

| Guardrail | Applies To |
|-----------|-----------|
| No raw colour pickers | Hero presets are named options |
| No per-segment styling | Itinerary colours derive from type + theme |
| No free icon picker | Content tile icons are fixed enum |
| No manual reordering | Itinerary segments ordered by time |
| No CMS-style versioning | Live edits, snapshot exports |
| Notepad never leaks to client | Admin-only scoping |
| No attendees on segments | People tagging lives only in Notepad |
| Stays belong to one section | No cross-section stays |
| Empty days render placeholder | Never blank |
| Flights = Travel segments | No separate hierarchy |
| No merged data sources | Unified via `portal_items` |
| No PDF pipeline | Print stylesheet only |

---

## Known Issues

1. **better-sqlite3 crash in Codespace** — environment-specific; not present on local PC
2. **Canvas system frozen** — 39 emerald remain, to be deleted
3. **`schema.sql` outdated** — missing newer columns; use Drizzle schema + `scripts/setup-db.js` + seed
4. **Missing DB columns on fresh setup** — `scripts/setup-db.js` handles all known columns; extend if new errors appear

---

## Development Workflow

**Editing:** Codespace (VS Code, Python heredocs, Copilot)
**Testing:** Local PC (no better-sqlite3 crash, real browser at localhost:3000)
**Sync:** `git push` from Codespace → `git pull` on local → test → report bugs

---

## Testing Commands

```bash
# Local first-time setup
node scripts/setup-db.js   # Create DB (schema + migrations + missing columns)
npm run seed               # Seed data

# Daily
npm run dev                # Dev server (port 3000)
npm run build              # Production build
npm start                  # Production server
```

---

## Current Session State

**Last completed:**
- Phase 6.1: Quick Wins (hero presets, icons, notices removed, member names)
- Phase 6.2: Itinerary Builder V1 (full stack)
- Phase 6.3: Admin Notepad (full stack)

**Recent commits:**
- `08c835f` — Admin preview feature
- `f329aa1` — Phase 6.2.5 itinerary editor
- `f0f1ce4` — Phase 6.2 complete
- (pending) — Phase 6.3 notepad commit

**Next action:** Pull locally, run full test pass, fix bugs, then Phase 7.3 production hardening.

---

## Phase 7.1 Test Session Plan

**Local PC testing:**
1. `git pull origin main`
2. `npm run dev`
3. Test each feature from the checklist above
4. Log bugs found
5. Report findings

**Estimated session time:** ~1-2 hours for thorough pass

---

## What's Left After Testing

1. **Loose ends** (3 items in 7.2) — small fixes
2. **Production hardening** (7.3) — Canvas deletion, deployment fixes
3. **Phase 8 features** — all deferred items

**Nothing urgent blocks launch after Phase 7.1 testing passes.**